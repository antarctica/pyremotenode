import logging
import os

from pprint import pformat


def reconstruct_files(input_files: list):
    """

    Args:
        input_files:

    Returns:

    """
    logging.debug("Got the following files: {}".format(pformat(input_files)))

    return []
